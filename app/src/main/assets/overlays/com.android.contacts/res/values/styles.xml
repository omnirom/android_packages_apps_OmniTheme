<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Contacts -->
    <style name="PeopleTheme1" parent="@*android:style/Theme.Material.Light">
        <item name="android:statusBarColor">@android:color/holo_green_dark</item>
    </style>

    <style name="ContactListFilterTheme" parent="@*android:Theme.Material.Light">
        <item name="android:statusBarColor">@android:color/holo_green_dark</item>
    </style>

    <style name="CustomContactListFilterView" parent="ContactListFilterTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:statusBarColor">@android:color/holo_green_dark</item>
    </style>
</resources>
